<view class="container">
  <!-- 用户信息卡片 -->
  <view class="user-card" bindtap="{{!hasLogin ? 'toLogin' : ''}}">
    <image class="avatar" src="{{hasLogin ? userInfo.avatarUrl : '/assets/images/icons/user.png'}}" mode="aspectFill"></image>
    <view class="info">
      <view class="nickname">{{hasLogin ? userInfo.nickName : '点击登录/注册'}}</view>
      <view class="desc" wx:if="{{hasLogin}}">{{userInfo.phone || '普通会员'}}</view>
      <view class="desc" wx:else>登录后享受更多优惠</view>
    </view>
    <view class="setting-icon" catchtap="toSetting" wx:if="{{hasLogin}}">⚙️</view>
  </view>

  <!-- 订单状态栏 -->
  <view class="order-card">
    <view class="card-header" bindtap="toOrderList" data-type="0">
      <text class="title">我的订单</text>
      <view class="more">全部订单 ›</view>
    </view>
    <view class="grid-nav">
      <view class="nav-item" bindtap="toOrderList" data-type="1">
        <image src="/assets/images/icons/cart-active.png" class="nav-icon" /> <!-- 替换为具体icon -->
        <text>待付款</text>
        <view class="badge" wx:if="{{stats.unpaid > 0}}">{{stats.unpaid}}</view>
      </view>
      <view class="nav-item" bindtap="toOrderList" data-type="2">
        <image src="/assets/images/icons/category-active.png" class="nav-icon" /> <!-- 替换为具体icon -->
        <text>待发货</text>
        <view class="badge" wx:if="{{stats.unshipped > 0}}">{{stats.unshipped}}</view>
      </view>
      <view class="nav-item" bindtap="toOrderList" data-type="3">
        <image src="/assets/images/icons/home-active.png" class="nav-icon" /> <!-- 替换为具体icon -->
        <text>待收货</text>
        <view class="badge" wx:if="{{stats.shipped > 0}}">{{stats.shipped}}</view>
      </view>
      <view class="nav-item" bindtap="toOrderList" data-type="4">
        <image src="/assets/images/icons/user-active.png" class="nav-icon" /> <!-- 替换为具体icon -->
        <text>待评价</text>
        <view class="badge" wx:if="{{stats.uncomment > 0}}">{{stats.uncomment}}</view>
      </view>
    </view>
  </view>

  <!-- 常用功能 -->
  <view class="menu-group">
    <view class="menu-item" bindtap="toCoupon">
      <view class="left">
        <text class="icon">🎟️</text>
        <text>我的优惠券</text>
      </view>
      <view class="arrow">›</view>
    </view>
    <view class="menu-item" bindtap="toAddress">
      <view class="left">
        <text class="icon">📍</text>
        <text>收货地址</text>
      </view>
      <view class="arrow">›</view>
    </view>
    <view class="menu-item" bindtap="toService">
      <view class="left">
        <text class="icon">🎧</text>
        <text>客户服务</text>
      </view>
      <view class="arrow">›</view>
    </view>
    <view class="menu-item" bindtap="toSetting">
      <view class="left">
        <text class="icon">⚙️</text>
        <text>系统设置</text>
      </view>
      <view class="arrow">›</view>
    </view>
  </view>
</view>